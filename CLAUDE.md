# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This is a newly initialized, empty repository. No source code, build tooling, tests, or configuration files have been added yet.

When the project is bootstrapped, update this file with:

- **Build/run commands** — how to install dependencies, build, and start the project
- **Test commands** — how to run the full test suite and individual tests
- **Lint/format commands** — the linter and formatter in use and their invocations
- **Architecture overview** — the high-level structure (monorepo vs single package, key layers, data flow)
- **Key conventions** — naming patterns, branching strategy, commit style, any domain-specific rules
